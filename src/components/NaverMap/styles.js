import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
    map: {
        width: '100%',
        height: '100%',
    },
    customMarker: {
        height: 50, 
        resizeMode:'contain',
    }
})

export default styles;